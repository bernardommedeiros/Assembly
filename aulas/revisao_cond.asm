.text 
main:
	 #beq -  checa se são iguais, se sim passa para o rotulo argumentado
	 #bne - checa se não são iguais, se sim passa para o rotulo argumentado 
	 
	 #ex: beq $8, $9, rotulo. Se o conteúdo de $8 for igual o de $9, passa a executar o que está presente no novo endereço
	 
	 #instrução j - recebe o endereço do rótulo, e salta independente de qualquer coisa para o rótulo anexado
	 
     #SLT -
     #compara dois registradores e salta para o endereço apontado caso o primeiro seja menor que o segundo
     #ex: slt $8, $9, $10. Se o conteúdo de $9 é menor que o conteúdo de $10, salta para o endereço apontado por $10 
	
	 
	 
	 
	  